# ZYREX PHASE 151–230

## 151–165 · Authoritative multiplayer layer
- Server-side transform clamping and validation.
- Server tick broadcasts authoritative room snapshots.
- Player input message with bounded throttle/brake/steering.
- Client prediction remains compatible with existing vehicle physics.
- Invalid/oversized state is rejected or clamped.

## 166–180 · Matchmaking
- Race, Sprint, Delivery and Speed Trial queues.
- Queue join/cancel/status endpoints.
- Small match groups are placed into a dedicated room.
- Existing room and WebSocket systems remain available.

## 181–195 · Friends & party
- Friend request / accept / remove.
- Pending requests.
- Party creation, invites, accept and leave.
- Persistent social data in `data/social.json`.

## 196–210 · Online missions
- Server mission catalog for Delivery Squad, Simpang Sprint and Night Run.
- Party/matchmaking modes map cleanly to online activities.
- Existing local mission/progression systems are preserved.

## 211–220 · Competitive validation
- Race cannot start before server countdown.
- Finish time has a server-side minimum and range check.
- Room capacity and host permissions remain server enforced.
- Transform/speed bounds are enforced on WebSocket and HTTP paths.

## 221–230 · Resilience & UI
- Competitive panel with matchmaking and social controls.
- Existing HTTP fallback stays intact.
- WebSocket reconnect remains active.
- Server snapshot correction telemetry is tracked client-side.

## Validation
- `node --check server.js` PASS
- `node --check public/game.js` PASS
- `node --check public/script.js` PASS
- Full browser/WebGL multiplayer runtime was not available in this environment, so it is not claimed as tested.
